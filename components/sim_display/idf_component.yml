## sim_display component - Display driver and LVGL integration

dependencies:
  # LVGL graphics library from component registry
  lvgl/lvgl:
    version: "^9.2.0"

  # Note: esp_lcd_dsi is a built-in component in ESP-IDF v5.3+
  # It provides esp_lcd_mipi_dsi.h and MIPI-DSI bus APIs
  # No external dependency needed - it's part of the ESP-IDF framework
