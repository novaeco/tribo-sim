version: "1.0.0"
description: "Tribo-Sim: Interactive reptile terrarium simulator for ESP32-P4"

# Declare external component dependencies for ESP-IDF component manager

dependencies:
  # LVGL graphics library v9.x
  lvgl/lvgl:
    version: "^9.2.0"

  # ESP LCD touch panel drivers
  espressif/esp_lcd_touch:
    version: "^1.1.0"

  # ESP-IDF core (implied, but explicit for clarity)
  idf:
    version: ">=5.3.0"
